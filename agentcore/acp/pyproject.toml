[project]
name = "agentcore-acp"
version = "0.2.0"
description = "ACP↔WebSocket bridge for Amazon Bedrock AgentCore Runtime"
requires-python = ">=3.11"
dependencies = [
    "bedrock-agentcore>=0.1.0",
]

[project.scripts]
agentcore-acp = "agentcore_acp:main"
